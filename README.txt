We have two part because we didn't merge the GUI and main code well.
So after type "make" and "make run", it will first run the GUI code.
As long as close the GUI window, you can play the complete game by text window.
